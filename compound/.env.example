# Use demo mode for local UI work without connecting to Supabase.
VITE_COMPOUND_DEMO_MODE=true

# Required when demo mode is false. These are public browser values, not secret keys.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
