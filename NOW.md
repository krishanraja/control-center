---
repo: krishanraja/control-center
product: Control Center
as_of: 2026-09-16
head: 6a7f9ffd
lifecycle: live
production_url: https://controlcenter.krishraja.com
state_doc: docs/plans/one-swing/STATE.md
history_log: docs/history/LOG.md
truth_files: [docs/icp.json]
authority_order: [production readback (Vercel deployment and Supabase), docs/MINDMAKE_OS_ARCHITECTURE.md, docs/DECISIONS, docs/plans/one-swing/STATE.md, README.md, docs/PRODUCT.md, docs/ARCHITECTURE.md, docs/GLOSSARY.md]
steward: https://github.com/krishanraja/control-center/blob/main/docs/steward/RUNBOOK.md
never_publish: [the Supabase project id, any credential or secret name, the cron secret, the private rate card, any named lead or room target, any figure from the scorecard or Compound]
---
# Control Center: where it is right now

## What it is

Control Center is the single pane of glass for mind/make OS, the fleet of AI agents that runs Krish Raja's business portfolio (Mindmake, the publication with its Paid and Built formats, mm-ctrl, Fractionl). A React and TypeScript dashboard on Vercel, reading Supabase directly and writing back through thin serverless routes, it exists to keep one promise: Krish opens one tab, sees every decision the OS is waiting on him for, decides in one click, and the rest runs in the background. The agents themselves are not in this repo; they are Supabase rows and n8n workflows. This is the dashboard slice, plus the crons that keep the OS honest.

## Who it is for and why it matters for Mindmake

Control Center is not sold. It is the proof asset behind everything Mindmake does sell. The pilot_face buyer (`docs/ICP.md`: a senior leader at a PE or VC backed media, adtech, publishing or data business, quietly behind on what is coming and unable to say so inside their organisation) is being asked to trust that one person can run a business on AI agents and stay in charge of it. This repo is the evidence, in public, with the failures left in.

Angles a writer can use without asking Krish:

- **The OS never pings him.** Since 6 September 2026 the OS is pull-only (ADR in `docs/MINDMAKE_OS_ARCHITECTURE.md` section 0b): every Telegram push was killed, including a fourth undocumented layer found in this repo's own API. Krish goes to the dashboard; nothing comes to him.
- **A green run with nothing sent is a failure.** The scorecard on Home counts approaches sent, calls, paid rooms, cash, pieces published, and hours building unasked. The last one is counted against him every Saturday from every commit in his repos, whoever the committer was, including this steward's own commits (ruling 7 September: "every repo in GITHUB_REPOS is his, so every commit in it is his build").
- **Operational exhaust is the content.** Since 7 September the same Saturday commit read feeds the Content Engine as build signals, and the architecture doc writes its own weekly changelog entry from them.
- **Self-healing is built in.** Four tiers of silent-failure detection surface value failures, not just exceptions. Several fixes in the last month are the OS catching itself lying: a Subscriptions tab that read as current while its ledger had been dead since July, a documentation sync that wrote to an untracked path for nine days, and, on 9 September, the tier-2 detector's own heartbeat call targeting a database function that had never existed, so the job whose entire purpose is catching silent failures had one of its own.

Objection it answers: "AI agents in a real business just make noise." Here is the dashboard that made them quiet.

## Where it is right now (as of 2026-09-15)

- **Live** at `controlcenter.krishraja.com`, auto-deployed from `main` on Vercel, Supabase behind it. CI runs lint, three typechecks, seventeen structural guards and a five-spec Playwright job on every push (`.github/workflows/ci.yml`).
- **API write routes are guarded.** As of 9 September, 75 of 141 `/api/*` routes that accepted a write with no credential now call the same access-code cookie check the web UI uses; a handful (health, sync, tasks-inbox digest, and reads on 18 dual-method routes) stay open by design for monitors and n8n. `/api/internal/sonnet-proxy` closed the same hole on 12 September (below). `docs/SECURITY.md` and `docs/DB_HEALTH.md` carry the full breakdown.
- **Harness learning inbox is live in production.** `harness_event_inbox` (Supabase, append-only, no anon/authenticated privileges) takes redacted event envelopes from `POST /api/harness/mcp`; three hashed emitter identities (Codex, Claude Code, Cursor) each hold an independently revocable credential. `krishanraja/ai-harness`'s GitHub observer imports the export into its evidence ledger and cannot write a skill, contract, registry or rule from it. ADR-020, `docs/plans/harness-learning-inbox/STATE.md`. The intermittently connected second workstation is still pending.
- **The internal Anthropic proxy now fails closed.** `/api/internal/sonnet-proxy` requires `Authorization: Bearer $N8N_PROXY_SECRET`; the previous version accepted any four-character caller header. `docs/MODEL_ROUTING_AUDIT.md` records the fleet's model-routing defaults and a 27-route Sonnet 4.6 migration queue, moved in cohorts, not in one deploy.
- **The one swing is the operating frame** (ADR-016, `docs/plans/one-swing/CHARTER.md`): one mission, five jobs in priority order. Jobs 1 (the Room) and 2 (the scorecard) are built and live since 5 September. Jobs 3, 4 and 5 are gated on the first paid room. Gate G1 is open; G2 to G4 are closed. Ledger and Vercel state: `docs/plans/one-swing/STATE.md`.
- **Growth is the last tab through the all-tabs rebuild** (`docs/plans/all-tabs-rebuild/STATE.md`, 10 September): it now shares the `DoThisNextHero` and `TriageDeck` primitives every other tab uses, its five sections run in the order one causes the next, and the creative board writes real scripts. The Room's proposal lane moved onto the same shared deck and now classifies who can actually buy (`ask_kind`/`ask_line`) rather than treating every name the same. That classification's migration (`20260910120000_room_ask_kind.sql`) is written but **not yet applied to production**: accepting a classified proposal will fail until it runs.
- **Content Engine**: one surface since 7 September. The v1 triage tab and its build flag are deleted; each of its features has a named home in `docs/CONTENT-ENGINE-PARITY-LEDGER.md`. Independent Money of AI and Built with AI lenses, one content output registry, approved ideas flow straight into Studio production, and the crons now write a run ledger the tab reads. Build signals ingest every Saturday 05:00 UTC; the first Saturday run has not happened yet.
- **Architecture doc**: `docs/MINDMAKE_OS_ARCHITECTURE.md` on `main` is the only OS architecture surface (ruling 7 September; VPS and Drive copies deleted). The engine writes section 20 on Sundays 13:00 UTC and stamps the header's "Last engine refresh" line each time.
- **Docs steward** (this file and `docs/steward/`) adopted 7 September (PR #279), bootstrapped by hand across all eight fleet repos the same day (`docs/steward/LEDGER.md`), automated by `.github/workflows/docs-steward.yml` on every push to `main` and nightly. Proven end to end on 7 September: the token gate, the digest, Claude's run and the strict validator all passed on a live dispatch. The `content-engine` fleet entry (renamed from `mindmake-video-studio` to match the sibling repo's own rename) was corrected in `fleet.json` on 14 September after the old name broke enrollment (PR #319).
- **Agent roster is 11 of 14 active.** Felix (2026-07-10), Kai (2026-09-07) and Priya (2026-09-14) are retired, rows kept for history. Hunter is not retired despite older notes in this repo saying so: it runs from GitHub Actions and is parked in the UI only, by Krish's choice. `docs/AGENTS.md`.
- **The n8n Anthropic fallbacks work now, and the runtime has been checked for secrets, not just the repo.** Gemini fallback branches were silently broken on 9 of 11 workflows (empty prompt in, unparsed reply out); every active Anthropic node now retries 3x with backoff, gated by `scripts/check-anthropic-fallback.mts` in CI. A first `scripts/n8n/scan-live-secrets.mjs` run (not a CI step; it needs a key that can read every credential-bearing workflow) found seven live nodes carrying real credentials behind clean git placeholders and, across the whole fleet, 217 secret literals in 66 workflows from 16 distinct live credentials, still owed a rotation.
- **Waiting on evidence, not code**: the scorecard's "hours building unasked" reads "not measured yet" until a Saturday GitHub read has written a week.
- **The Network lane now guarantees one click to LinkedIn for every contact, whether or not a profile URL is on file** (ADR-021): a real profile when the URL exists, otherwise a labelled, dashed, last-sorted, speculative pre-filled LinkedIn search, never presented as verified. Enrichment reaches search by trigger rather than convention (ADR-022): typed `contact_intelligence` columns, a 0-100 `completeness` score, and `embed_stale` (never a nulled embedding) replace the untyped `contacts.raw` jsonb the ranker never read; a mandatory contact-import gate now checks an incoming LinkedIn URL after 1,381 of 4,105 Apollo-file URLs turned out to belong to someone else. Intent scoring is an eight-tier stance ladder (asking to selling) with a null-when-unknown score and a Haiku-verified false-positive filter, not the single AI-posting flag it was a day earlier.
- **Contacting anyone the Network surfaces is one click, channel-aware, and discloses what will happen before the click** (PR #324, Krish's own words: "every network suggestion in controlcenter should be one click to contact them"); the same logic backs BridgeCard, and the `mindmake_wedge` tier ("their call either way") landed with it.
- **Live**: the hunter job-search lane (Hunt, on People) is always on as of 2026-09-15, e2e-tested at 390/820/1440px (PR #323). It was hidden behind `VITE_BRIDGES_LANE_ENABLED=false` in production from the 2026-09-06 parking, and stayed hidden for eight days after the 09-07 code-level un-parking because only the default changed; the flag is retired, not re-defaulted. An applied role now reads "Applied" with its date, and the card no longer builds a LinkedIn URL from a bare `contact_key` for someone with no stored profile. A browser-extension payload route (PR #336) lets Krish, on Windows with no Python or terminal, fill an application and confirm the Submit press himself (PR #341); a superseded application link now reads a plain next-step page instead of a 404 (PR #339).

## What changed recently

- 2026-09-16 **"The Room" is now Pilots, the lane fits one phone screen, and the enrichment finally reaches the rest of the app.** Why: Krish opened People, Room on a phone and read "Ask David if he knows an agency leader who should hear about the room" on a card, under five bands of chrome, needing a scroll before the first name was fully visible. The sentence was not in the source: it is written per person by the seed route's classifier, which interpolates `DOOR`, and `DOOR` opened "A confidential room", so every prompt inheriting it repeated the word at a reader who had never been told what it meant. His ruling: rename, all the way down, and it is about getting **pilot customers** for Mindmake. `DOOR` now says "a paid three week pilot" and carries the ban itself; `room_targets` is `pilot_deals`, the ladder states, the scorecard column, the job ids and `/api/room/*` moved with it, and `?lane=room` still resolves so old links work (ADR-023, migration `20260916100000`, verified by dry run inside a transaction against production data before it was applied). The phone lane is now a fixed `100dvh/--z` frame showing ONE person on a stage with a pager, the long judgment folded behind a disclosure and the draft body moved into `FocusedEditor`; density has to come from disclosure because `index.css` neutralises `.truncate` and every `.line-clamp-*` on purpose. Pinned by `e2e/pilots-noscroll.spec.ts` at 390x844 and 360x800, with the sub-700px case asserted to scroll rather than clip the action row. Separately, the 14-15 September enrichment chain (ADR-022) had only ever reached the Network lane: the Pilots seed received `intent_evidence`, `intent_stance`, `followers` and `completeness` from the same scorer and mapped six fields of forty-four, so a card said "No live trigger found" about someone whose source-checked quote was in the same database, and `findTrigger` paid for a fresh web-research call per person to look for it. Nine more consumers were reading the 2026-06 spine: outbound email grounded on the legacy `dossier` blob, the tab-grounding block reported a capped 2,000 as the corpus size against 11,755, the relevance sweep filtered after an unordered `limit()`, the Leads tab shipped a `vector(1536)` to the browser and filtered a truncated page, and 119 contacts had no `contact_intelligence` row at all, which means invisible to every surface that goes through the scorer rather than low-ranked. All fixed and read back: 0 invisible contacts left, all 119 with a composed `intel_doc` and queued for embedding. Two data problems found and flagged rather than silently patched: `contact_intelligence.tier_weight` carries two scales at once (`2_core_network` appears as both 3 and 85), and four intent columns are written, constrained and read but added by no migration, so a clean `supabase db reset` fails. Migrations `20260916100000`, `20260916110000`, `20260916120000`.

- 2026-09-15 **The "one click to LinkedIn" promise is now a rendering guarantee, not a data target, and 1,037 Circle-community contacts landed in the network.** Why: measured against the corpus the day this was decided, only 4,941 of 10,768 contacts (46%) carried a LinkedIn URL, and three quarters of the rest were labelled only with the identifier of the import run that loaded them; no enrichment budget was ever going to drive 5,827 missing URLs to zero. Every result row now renders either the real profile or a labelled, dashed, last-sorted, speculative pre-filled LinkedIn search, never presented as verified, and the same rule covers 150 pattern-guessed Circle-import email addresses, surfaced as "unverified - may bounce" and kept out of `email_normalized`. `contactProvenance` now prefers a human-typed campaign name over a machine slug like `network_intelligence_2026_08` and says "Unknown source" rather than implying a shared context that does not exist. A same-day follow-on moved surname repair from a CLI script into the tab itself after a live run found a missing cursor meant a second pass returned zero of 1,167 remaining candidates; with real paging, 55 of the first 200 recovered. ADR-021, migration `20260914210000_network_search_provenance.sql`. PRs #321, #322.
- 2026-09-15 **Money spent on enrichment had stopped reaching the app.** Why: 1,700 Coresignal credits bought full LinkedIn profiles for 169 contacts, and every one of the 169 still had a `contact_intelligence.updated_at` predating its own enrichment; 90 stayed badged "thin evidence... no profile was ever read" with a complete profile sitting in the same row, because rebuilding the row's search text after a write was a convention every importer had to remember, not something enforced. Fixed with typed `contact_intelligence` columns, a trigger that recomposes search text and marks embeddings `embed_stale` (never nulled, so a stale vector still finds someone on old text while a fresh one queues), and a 0-100 `completeness` score weighted toward what is actionable rather than what is populated; the 169 profiles now average 94/100. The same five-PR chain found 1,381 of 4,105 Apollo-file LinkedIn URLs belonged to someone else (370 provably swapped between named rows) and added a mandatory import gate, fixed a batch runner that stopped a 1,300-person run after 15 refusals because one exhausted provider looked like every provider failing, and corrected a same-day pricing miss ($0.0013/profile estimated vs $0.0090 actual) that ran a $58 bill against a $29 plan. ADR-022, migration `20260915140000_enrichment_reaches_the_surface.sql`. PRs #325-329.
- 2026-09-15 **Intent scoring stopped asking the wrong question.** Why: the first model asked only "is this person posting about AI" and flagged 307 of 677 warm contacts - "the right answer to the wrong question" - while a second pass was finding classifier false positives at 95/100 confidence on nostalgia and job ads. Replaced with an eight-tier stance ladder (asking, struggling, hiring, evaluating, building, teaching, commenting, selling) plus a two-layer filter (free pattern gating, then a Haiku call on the ambiguous third, every quote checked verbatim against source before storage), cutting flagged volume from 12% to 6% of the corpus and turning all six known false positives into regression tests. 77 leftover rows from the retired first model (a score with no stance, evidence or posts) are nulled rather than zeroed, with a CHECK constraint now requiring stance and score together. A same-day fix wired the computed `intent_stance` into `network_search`'s own response, since it was being computed but never returned - "the five people actually stuck on an AI rollout never surfaced above eleven thousand others." PRs #333, #334, #335, #337, #338.
- 2026-09-15 **Two silent bugs let intent scoring report success while doing nothing.** The posts actor returns `postedAt` as an object, not a string; the first parser read it as a string and got nothing for 8 of 8 scraped people, making every intent score in that batch "a structural zero while the run reported success" (PR #331; a narrower same-day fix, PR #330, recorded the actor's field names instead of guessing so a missing date reads as missing rather than silently unread). Separately, the enrichment route embedded the doc it composed rather than what the database recomposed from the written columns - caught when two people flagged "Posting about AI agents" still showed a row claiming its embedding was current; a mismatch now marks the row `embed_stale` instead of asserting freshness it does not have (PR #332).
- 2026-09-15 **A degraded enrichment stopped calling itself clean, and a contaminated benchmark got corrected the same day it was made.** `enrich-person.ts` told API callers the truth (`enriched_degraded`) but wrote a flat `enriched` to the row, so every surface reading the row believed a clean enrichment - the exact honesty gap `check-enrichment-honesty` had been red about since PR #340 (PR #343). A Network tab search timeout (7.8s search against an 8s cap, lexical recall alone 3.49s over 3,540 rows) traced to enriched `intel_doc` rows averaging 1,488 characters against 423 unenriched, fixed by capping retrieval text and the recall pool (PR #344), then partly retracted the next commit when the pool-size benchmark that justified 250 turned out to have been measured while a bulk re-embed was saturating the same database; on an idle instance the gap disappears, and 250 stays justified only by an identical top-twenty down to 150 (PR #345). A `PersonDetail` type error left main and every PR red on a property that did not exist on the interface, invisible to Vercel's build because `tsc` is not part of it (PR #342). Migrations `20260915250000_cap_retrieval_text_length.sql` and `20260915250000_enriched_degraded_is_a_status.sql` share an identical timestamp prefix from two unrelated PRs - a naming collision worth a note, not a defect.
- 2026-09-14 **Kai's retirement never reached the database row, Hunter was wrongly declared retired in three files, and a first runtime secrets scan found seven live n8n nodes carrying real credentials behind clean git placeholders.** Why: Kai was retired 2026-09-07 and its workflows archived, but the `agents` row stayed `active = true` with `expected_runs_per_day = 6` and no run since 08:00 that day (135 runs the month before, then nothing), the exact pattern the fleet observer reads as a dead agent rather than a completed retirement; it is now flipped and out of the inbox classifier enum, the orchestrator router, the workflow map and Leo's weekly roster, and Felix (retired since July) came out of the same three places. Hunter was marked retired in `api/agents/[name].ts`, `loadingVoice.ts` and `docs/AGENTS.md`, but it ran 11 times in the last 30 days, most recently that day, from GitHub Actions rather than n8n; the comments were stale, not the agent, and all three are corrected (`agents.active` stays true, Krish's call to flip it). `scripts/n8n/scan-live-secrets.mjs` asked the runtime the same placeholder question `check-no-secrets` asks the repo, since the repo can only see itself: seven live nodes were carrying real keys as header literals while git showed a clean `{{PLACEHOLDER}}`, including a live Anthropic key in Nova's Podchaser filter and service-role JWTs across five Acquisition Reply Intake nodes; all seven now use the shared credential objects. The first full run found 217 secret literals across 66 workflows and 16 distinct live credentials; rotation, not a placeholder swap, is the only fix that counts. PR #320, `766b5f59`.
- 2026-09-14 **The Anthropic fallback repair reached n8n Cloud for every workflow, applied node by node so no placeholder ever sat near a live credential.** Why: a repo fix means nothing until the running copy matches it. 31 workflows were updated and published through the MCP server first, four Maya lanes verified live by reading the published version back (retry set, `gemini -> adapt -> merge` wired, adapter reading `candidates[]`, builder pinned to a prompt node); the six workflows whose MCP access toggle was off are named in `scripts/n8n/README.md` so drift on them reads as known rather than mysterious. Once that toggle was switched on, the last six went through the public API instead, verified live on all six (retry settings present, Priya gone from the inbox classifier enum, the published version is the active one). `cleo-synthesis-engine`'s cloud copy had pre-dated the repo file entirely, still calling its Anthropic node "Opus Synthesize" while sending `claude-sonnet-4-6`; the live node is renamed to "Sonnet Synthesize" so the name matches both the model and the file. PRs #317, #318; `57dc1c39`, `6e58c7f0`.
- 2026-09-14 **Nine of eleven Gemini fallback branches in `scripts/n8n` could not have worked, and the failure was silent: the run stayed green and the output went empty.** Why: the body builder read `src.anthropic_body` off the error item, but every workflow keys its request differently and n8n's error output does not carry the input json anyway, so Gemini was asked an empty question; the parse node then read Anthropic's `content[0].text` shape against a Gemini reply shaped `candidates[0].content.parts[0].text`, so it found nothing either way. Builders now pin the prompt node by name and find the body by shape, and a new "Adapt Gemini" node converts the response shape and stamps `_llm_provider` and `_degraded` so a Gemini-written draft cannot pass as a Claude-written one. Every active Anthropic node now retries 3x with backoff, since a 529 overload had been killing 20 workflows with no fallback and no retry; `api/_retry.ts` gives the API side the same policy, wired into `callClaude`, `callClaudeMessages`, the relevance sweep and the n8n proxy. `scripts/check-anthropic-fallback.mts` gates all of it in CI. Priya is retired: her twice-daily scan produced a Google Doc bug report and three Telegram messages a run that changed no decision, since `fleet-reconcile` and the Vercel deploy checks already cover what she measured; both workflows are unpublished and moved to `scripts/n8n/_retired/`, `agents.active` is false, and 33 stale health-alert tasks are superseded. PR #316, `ae7757a0`.
- 2026-09-13 **One spelling for the text hierarchy, and a Claim primitive extracted from Focus and Purpose.** Why: Krish named two problems the same day, that the design system does not feel standardised outside Focus and Purpose, and that what the OS proposes reads as relentlessly serious ("the content suggestions are so serious and intense", Krish, 2026-09-13). The `text-strong/muted/faint` component classes and the `text-ink*` Tailwind tokens were the same three colour channels under two names: 23 call sites used the classes, 21 files used the tokens, and 185 of 257 component files used neither and stayed on `text-white/NN` opacity soup. The tokens win, since they take opacity modifiers like any other utility; the three classes stay defined so an unswept file cannot render colourless, and a third invariant on `scripts/check-type-tokens.mts` stops the fork coming back. `<Claim>` (`components/shared/Claim.tsx`) extracts the one serif recipe Focus and Purpose already had right (Newsreader, a role size, 1.4 leading, `text-ink`, a micro source line, never clamped) out of twenty call sites that had been carrying six variants of it. Proposal prompts (`proposalPlay()` in `api/_humor.ts`) now spend one proposal per batch on a marked swing, labelled "The wild one", so a prompt built only from prohibitions stops being the whole of what the OS offers him to choose from. `9df9bef9`.
- 2026-09-13 **Studio art direction is reviewable inside the one Content Engine UI** (`codex/art-director-repertoire`, awaiting PR). Why: a deterministic art director is only useful if Krish can see the visual choice without entering a second dashboard. Treatment and final review cards now show compact beat-level choreography, no more than two alternatives and a simplify action. Either edit opens the existing magic direction sheet with the exact instruction prefilled. A genuinely new mechanism is labelled as the sharp alternative and cannot pass as an ordinary effect: the card states that styleframes and an animatic are required. Long text wraps at phone and desktop widths, and the cloud projection still excludes transcripts, local paths, source media and private artifacts.

- 2026-09-12 **The internal Anthropic proxy accepted any four-character `X-Internal-Caller` value, so any caller who found the URL could spend the server's Anthropic balance; it now fails closed.** Why: `/api/internal/sonnet-proxy` requires `Authorization: Bearer $N8N_PROXY_SECRET`, keeping the caller header for attribution only. The same audit right-sized the two highest-volume n8n routes: the Inbox Classifier moved from Sonnet 4.6 to Haiku 4.5 (a classifier, not synthesis) and the Task Lever Rater moved from Opus 5 at a 16k output cap to Sonnet 5 with thinking disabled and a 2k cap, for at most 20 short rows a run. Twenty-seven other active workflow files still call Sonnet 4.6 and stay there until each route is scored on at least 30 historical inputs, because Sonnet 5's tokenizer produces roughly 30% more tokens for the same input and non-default sampling parameters now return HTTP 400. `docs/MODEL_ROUTING_AUDIT.md`. `a671b053`.
- 2026-09-12 **The harness now has one remote inbox instead of per-machine collectors, and it went from code to a verified production loop the same day.** Why: most interactive AI surfaces wrote nothing back to `ai-harness`'s learning loop, and a per-machine daemon would have multiplied operating systems, schedulers and credentials. `harness_event_inbox` is append-only with no anon or authenticated privileges and no update/delete grant even for the service role; a first production readback caught default `REFERENCES` and `TRIGGER` grants slipping through, fixed by a same-day least-privilege repair migration. `POST /api/harness/mcp` is now the preferred client boundary: three hashed emitter identities (Codex, Claude Code, Cursor) each get an independently revocable credential and a 30-event daily limit, and a second fresh Codex session picked up the capture rule with no explicit instruction, which is the behavioural trigger proving itself rather than just the transport. The GitHub importer advanced the observation ledger's cursor from 1 to 9, touching only `state/observations` and no contract, skill or rule. ADR-020, `docs/plans/harness-learning-inbox/STATE.md`. PRs #310, #311, #312, #313; `47179b4b`, `a6115f70`, `0b41c0e0`, `a7d0595b`.
- 2026-09-11 **Krish opened the shipped build on a phone and reported five things wrong, several of them his own earlier calls being overruled.** Why: "the swipe cards get in the way of the rest of the user experience on mobile", "the Do this next, read the review button doesn't actually work", and "more than half the screen is fixed, which is ridiculous" (Krish, 2026-09-11), each verified in code before anything changed. The Room's three-paragraph purpose block is one line now, with the offer and the 25/5/1 arithmetic folded behind a disclosure. The phone swipe deck owns the whole screen (`MobilePilots` switches to a `scroll="none"` shell while it is up) instead of sitting in a fixed 540px box that fought the page scroller for the same drag. Known collaborators still appear but now sort to the bottom of the deck, after everyone who could actually sign - Krish's call after seeing all three options on the table. A candidate with no company and no title cannot be judged, so `/api/pilot-deals/seed` now looks such candidates up (capped at two paid-provider calls per press) and drops whoever is still unidentifiable, saying the count out loud rather than quietly returning four cards out of five; a judgment that cited "already in Krish's contacts" as evidence no longer survives into the card, since everyone shown is in the contacts by construction. The Growth hero's "Do this next" button used to only set the section, so pressing it while that section was already open did nothing; it now points at the first review still owing a ruling and opens it. On a phone, Growth also loses the purpose sentence and the section-purpose line, since the hero already says what to do and the two lines were costing more than half the screen before any content. No migration in this change; CI green on both jobs including e2e, the first fully green run on the branch. `a1787b74`, `edc9f515`.
- 2026-09-10 **Four e2e specs were red on main for a day because the fixtures went stale, not the product.** Why: PR #307 (9 September) added `shiftIsOnBeat`, the filter that only lets a shift reach a room when it carries one of the six live lenses. `e2e/content-rooms.spec.ts`'s fixtures still carried only the retired `category` vocabulary with no `lens` field at all, so both rows were filtered out and every assertion after that looked for a card the component was never going to render. The file's own header comment, claiming governance, security and org "cut across both formats by design", was also the opposite of the design: those three were retired precisely so they would stop being produced. Both fixtures now carry a real lens; content-rooms is 4 of 4 again. `da05487a`.
- 2026-09-10 **The Room says what it is on a phone, records real verdicts, and knows who can sign; Growth gets the spine every other tab already has.** Why: Krish opened production on a phone and could not tell why anyone was in the Room or what to do with them, and Growth read as "not very guided and not very sequential, just loads of random things to do everywhere" - the one tab that never went through the all-tabs rebuild. A Skip in the Room used to filter a local array and write nothing, so the same five candidates came back on every reload and nothing Krish decided ever reached Vera; it now writes a `not_now` row plus a coded `feedback_queue` vote. The list also could not tell a person who can sign a fixed fee from one who can only open a door - it had proposed Krish's own podcast co-host as a sales prospect - so `pilot_deals` gains `ask_kind` (buyer / intro / collaborator) and `ask_line`, classified by `/api/pilot-deals/seed` from fields the search already returns (migration `20260910120000_room_ask_kind.sql`, not yet applied). Both lanes now render through the shared `TriageDeck`, the phone counterpart to `SwipeCockpit` that never existed before. Growth's five sections now run Review, To do, What's moving, then the two references, landing on the week's work instead of the map; the creative board writes real scripts (`/api/growth/clip-ideas`, `/api/growth/clip-script`, two calls in series, an optional humour register) rather than staying empty as its own header had always claimed it would. Also fixed in the same run: two competing bottom-nav active marks (one scaled inside the icon wrapper, one a hardcoded mint glow that stayed mint on paper while the ramp around it flipped to deep green) collapsed into one theme-following pill. `5bc3b1aa`, `45e4347c`, `b7097f8b`, `b5fca137`, `ca3be6aa`, `684aabe2`.
- 2026-09-10 **Two documentation claims about CI were wrong and are corrected.** `AGENTS.md` and `docs/TESTING.md` both said CI runs no Playwright at all; `.github/workflows/ci.yml` has run an `e2e` job alongside `verify` since before this range, covering five specs (content-rooms, content-queue-window, composer, video-engine-mobile, growth-scroll). Found while fixing the growth-scroll regression in `ca3be6aa` ("I repeated that" claim, then checked it). Both files now name the five specs and say the rest of the suite is local-only.
- 2026-09-09 **The detector for silent failures was itself failing silently.** Why: `public.log_workflow_run` did not exist anywhere in the schema, yet the Silent Success Detector, the workflow whose entire job is catching jobs that run and do nothing, had been calling it at the end of every run for months. Found while auditing detection coverage, not from a report. The function now exists (`SECURITY INVOKER`, deliberately not definer, since the caller may hold the anon key), verified twice through the real REST endpoint. What it does not fix: 269 unresolved `silent_failures` rows, oldest from 15 June, and `escalated_to_krish_at` has never been set once in the table's history. PR #309.
- 2026-09-09 **Guard the write routes. The API was open to the internet.** Why: 141 API routes, only 44 guarded; 75 accepted a write with no auth check at all, 22 of them reaching an outbound dispatcher on the service-role key, which bypasses every RLS policy. `/api/agents` returned 268 KB of agent briefs to an unauthenticated fetch. Every route now calls the same cookie check the web UI uses, except health, sync, sonnet-proxy and tasks-inbox digest (for monitors and n8n) and reads on the 18 dual-method routes, by Krish's ruling. PR #308.
- 2026-09-09 **Content tab: stop the two halves fighting, and filter the noise.** Why: the obligation strip and the room below were two independently height-capped scrollers, so five proposals overflowed their cap and got cut off mid-sentence while the room had nothing to fill its own space; the tab also opened with five machine questions and none of Krish's own work on screen. Four data defects were fixed underneath: room badges and rooms disagreed on which cards counted, so two rooms read empty while nine drafts sat buried; `shiftIsOnBeat` now filters shifts to the questions this desk actually tracks after the August rewrite retired the old categories; `contentRouting.ts` gives every one of the roughly 119 ideas with no stored route a decision (about 40% of the corpus is governance and safety news that fits neither masthead, so the router refuses those by name rather than splitting them evenly); and a radar signal the lens never answered for is now recorded as `unjudged` instead of being coerced into a false `no_angle`. PR #307.
- 2026-09-09 **Close the anonymous write holes, and stop the alarm writing into a void.** Why: reading `pg_policies` directly found 24 tables with an anon or public write policy nothing in the codebase ever used, including `standards_registry` (169 rules) and `agent_plans` as `ALL to public`, writable by anyone who read the anon key out of the deployed frontend. 20 revoked outright, 2 narrowed to the single update the dashboard performs; `workflow_runs` stays open pending an n8n credential check. Separately, Krish ruled the weekly failure-pattern alarm off: 11 `corrections` rows had sat unread for up to 17 days while the underlying problem got worse, so `audit_failure_patterns()` still detects and clusters but no longer writes a row nobody reads. PR #306.
- 2026-09-08 **The docs steward installer had no directory to install into.** Why: `anthropics/claude-code-action@v1` installed Claude Code, then failed with "native binary not found," after its own log had already said the launcher directory did not exist and then declared success anyway. Reproduced twice (the nightly run and a same-job re-run both failed identically), so a single `mkdir` before the action runs, not a version pin, is the fix. All ten fleet repos share this workflow, so all ten nightly docs stewards were failing and are fixed by this one change.
- 2026-09-08 **Run again: make the obligation strip actionable.** Why: the strip could say a job was stale and offer nothing to do about it, so a job that failed on Friday waited a week for the next scheduled run. Every engine attention row now carries a Run again button wired to that engine's own replay route; all 17 job names are checked against the replay registry so no row can offer an action the engine will not recognise. PR #305.

- 2026-09-08 **The outermost clock, outside the thing it watches.** Why: the harness watchdog lived in the repository it watched, so a repository-wide failure would take the alarm with it. `api/health.ts` now reads `state/heartbeats.json` from `krishanraja/ai-harness` over the GitHub API and raises a critical alert when the newest heartbeat is over 48 hours old. It cannot be silenced by the thing it measures, which is the only property that makes a watchdog worth having.
- 2026-09-08 **Prompt-cache spend is visible, and guarded.** Why: five Anthropic call sites each plucked `input_tokens` and `output_tokens` by hand and dropped the cache fields, so a cached call and an uncached one of the same size were identical in `meter_daily`, and caching could break with no error and no symptom except a larger bill. `api/_meter.ts` now takes the whole usage object at one choke point, `api/_prices.ts` prices reads at 0.1x and writes at 1.25x and 2x, and `meter_daily` carries `cache_read_tokens`, `cache_write_tokens` and `usd_uncached` so a site writing caches nobody reads shows up as costing more rather than netting to zero.
- 2026-09-08 **Measured, and the answer was no.** Why: the caching plan assumed there were prefixes worth caching here. There are not. The largest constant Anthropic system prompt in `api/` is roughly 842 tokens against Sonnet 5's 1,024 token minimum, so a `cache_control` block today would be silently ignored and cost exactly the same while looking like a saving. `scripts/check-cache-metering.mts` reports that as an advisory on every push and will say so the month a prompt crosses the line, because nobody is going to remember to re-measure.

- 2026-09-09 **The AEO research machine lands: subjects, probes, digests, and recommendations into the content spine.** Why: Krish wants the white-glove AEO research agent from a LinkedIn post for every venture, every company he wants to sell to and every company he wants to be like, autonomous and always ending in an action. The machine is `krishanraja/AEO-Engine` on GitHub Actions (Sunday 04:00 UTC); this repo gained the subject registry, the extended probe table, the query corpus and digest tables, five routes under `api/aeo/`, `aeo_signal` in the content spine, and the council's AEO evidence block. The Signals surface is rebuilt after a rendered mock is approved. Spec: `docs/AEO-ENGINE.md`.
- 2026-09-08 **Growth says what it is for and reads insight first.** Why: on a phone the + opened the desktop grid inline with a native select and a date picker, the GEO rows spiralled into a three-word column beside a chip stack, the weekly review buried the council's own headline as one key: value row among eight, "autonomy ladder" explained nothing, the connected tools truncated their jobs to three letters, and Spend limits read $0 while Intel read the real bills with no line between them. Now the header carries the purpose, every evidence section opens on one sentence with the rows folded, each council move can become today's work or a clip from the card, the two composers are house bottom sheets with chips, and Spend limits shows Intel's number beside a line saying what the lane figures count.
- 2026-09-08 **No magic sparkle icons.** Why: eighty call sites reached for Sparkles to mean "the machine did this" and Wand2 to mean "refine or enrich this"; both read as magic, and nothing here is magic, it is a model call with a cost and a wait. The names stay in `@/lib/icons` so no call site changes; the glyphs are now Cpu (machine work) and PenLine (an edit), recorded in the design system's iconography rules.
- 2026-09-08 **Every wait in the daily loop now has words, an end, and a way out.** Why: an audit of boot, check-in, Home, Today, the ritual, the lock and the shutdown on a slow or absent connection found a hung request left a disabled button and a spinner with no end, the boot held a breathing splash for six seconds then a blank screen, an offline write hung until the OS gave up, and the calibrator lock could run ninety seconds with no sentence. One timed, offline-aware request helper (`src/lib/apiFetch.ts`) now backs every operator-facing call: it refuses at once when offline, times out with a plain sentence, and every wait narrates with an elapsed clock and a door past the usual time.
- 2026-09-08 **The phone pass after the unification.** Why: Krish opened production and saw video cards that were the 5 September synthetic validation rows with a live decision button and no media, a lockup offset outside its card, the Growth list cut off above the nav, and an empty Room with no reason. The two synthetic jobs are retired (`video_studio_jobs.retired_at`, kept, never shown), a treatment or final decision needs a preview to judge, the preview state is said in words, the nav clearance sits on the scrollers, the Room reads its errors and finds five on first open, and a Studio brief says what the runner did with it. `docs/plans/video-engine/STATE.md` now says the control plane is live.
- 2026-09-08 **Shutdown on a phone: the way out is always on screen.** Why: seen live on a phone, the sheet scrolled as one piece so the close control scrolled off the top, and the dialog painted under the bottom nav, so "Not now" was buried under the tab bar with no way out. The shell is now a pinned header and footer around a scrolling middle, at the same layer as the bottom sheet, so both exits stay on screen.
- 2026-09-08 **Goal reads and writes carry the device zone.** Why: the weekly objective create, the Carry patch, the ladder read and the history read resolved the week from the stored operator setting alone, a second path from the one every pilot and Today call already used, with a 60 second server cache between them. Every goal call now sends the device's timezone the way `usePilot.ts` does; the stored setting is the fallback only for callers with no browser (cron, n8n).
- 2026-09-08 **Goals canon aligned: shutdown feeds tomorrow, weeks close on Saturday, Today is manual first** (ADR-018). Why: the shutdown prompt returned on every new tab (sessionStorage), no evening row had ever been saved, `daily_focus` was empty, Home carried two weekly composers (one expanded inline on a no-scroll screen), and nothing ever closed a week. One writer for tomorrow's 3 (`api/_dailyFocus.ts`), `goals.week_start` plus a Saturday `week-close` cron and a `missed` status, editable Today slots, the ritual as the one weekly composer, and the goal spine now carries today's frame and the last four weeks into every Ask.
- 2026-09-07 **One Content surface, nothing lost.** Why: three generations of Content tab shipped in one bundle behind a flag that was not in `.env.example`, so a fresh deploy served the retired triage pile; three candidate models rendered in one column; the video engine had no desktop path. The v1 files are deleted, the parity ledger names where every feature went, the lane reads in the order you act, the Composer rail is three stages, the phone deck clears the upstream pile, and learning proposals from the Studio have a reader. Hardening in the same change: a `content_engine_runs` ledger written by every content cron and read by the obligation strip, `feed/ingest` fails instead of skipping when unconfigured, a daily watch on the Windows runner, an env-example guard, and the four content and video Playwright specs now run in CI.
- 2026-09-07 **The scorecard counts every commit, not one login** (`37af6d3c`, guard `e86e31b8`). Why: a dry run showed most session commits carry the coding agent's address (36 of 38 in one repo, 18 of 34 here), so the Rule 6 tripwire "was reading roughly half the truth". `GITHUB_AUTHOR` is no longer read anywhere and the guard fails if either reader brings it back.
- 2026-09-07 **Architecture doc: the one-surface retirement closed out** (#280, #281). The last three references to a VPS or Drive copy removed; the three weekly Documentation Refresh Routines recorded as deleted by Krish; the fleet's last reference to the trashed Drive file removed.
- 2026-09-07 **Build signals** (`2c83ff90`, `e11f2ab5`). Why: every Saturday the scorecard counted Krish's commits against him as unasked hours, and nothing read the same stream for content, although canon asks for a solo Built with AI variant on exactly that material. One new `source_type` flows into the existing content spine; nothing new to run.
- 2026-09-07 **One architecture surface, kept current by the engine** (`ec7f05e7`, `31fc3162`, `1be3dddb`). Why: six copies of the OS architecture doc drifted silently; the sync script had written a misspelled filename into an untracked path for nine days and nothing errored. Krish deleted the VPS and Drive copies for good. A Sunday cron now writes the weekly entry.
- 2026-09-07 **Unified Content system** (PRs #274 to #278). Editorial radar with independent lenses, one output registry, approved ideas joined to Studio production.
- 2026-09-07 **People: one graph, three doors, a Hunt lane** (`1410ae8c`). "Process my verdicts" fires a repository dispatch on the job-search tool so a run starts within a minute.
- 2026-09-07 **Subscriptions read as stale** (`e960bb4f`). Why: the customers ledger had not been written since 17 July because a nightly n8n reconciliation had errored on every run after an account key expired, and the header still looked fresh.
- 2026-09-07 **n8n git and cloud parity rebuilt, Kai retired, secret guard hardened** (#270). Why: git claimed to be the source of truth while 43 snapshots stood against 123 cloud workflows with 154 drift items, so an apply would have overwritten live definitions with stale ones.
- 2026-09-06 **Pull-only OS** (#267, then #271, #272). Every Telegram push killed, including four callers in this repo's own API; the Maa reminders switched off and the doc that named them an exception corrected.
- 2026-09-06 **One filename for the architecture doc** (#268, #269). Why: the repo tracked `MINDMAKE_`, the VPS and skills tracked `MINDMAKER_`, and surface 2 diverged from the rebrand for nine days without an error.
- 2026-09-05 **The one swing shipped** (`efcfe51e` to `e1f81545`): canon repointed to Ikigai v4, the Room (25 leaders who fit the face, drafted by the OS, sent by Krish), the scorecard, the demand-engine feed, the job search parked, and a runway sentence in COMPOUND.
- 2026-09-04 **COMPOUND Spend and Property tabs** (#253 to #259) and the **Video Engine control plane** (#260 to #266).
- 2026-09-01 to 02 **Bridges lane** for warm-intro candidates and a gated inspiration lane from favourite creators.
- 2026-08-29 **Mindmake rebrand** of the OS and the architecture doc; a committed Telegram bot token removed and a guard added so the next one fails the build.

## What is next and what is waiting on Krish

- Next: jobs 1 and 2 of the one swing are the work in play. `docs/plans/one-swing/STATE.md` carries the ledger; the Monday note carries the week.
- Waiting on Krish (`docs/plans/one-swing/STATE.md`, "Ops steps owed"): `PARTNER_EMAIL` when a partner exists; whether to set the hunter agent inactive; a cash balance in COMPOUND Settings; saying the mission out loud to one person.
- Waiting on Krish, found 2026-09-14: rotate the 16 live credentials `scripts/n8n/scan-live-secrets.mjs` found exposed as header literals across n8n Cloud (217 secret literals in 66 workflows). Moving a workflow onto the shared credential object stops the leak going forward; it does not invalidate a key already sitting in n8n's own version history.

## Read next

1. `docs/MINDMAKE_OS_ARCHITECTURE.md`: the whole OS. When this repo's docs disagree with it, it wins. Read sections 0a to 0c first; they override older prose below them.
2. `README.md`: what this repo is and should be, the tech stack, the layout.
3. `docs/plans/one-swing/CHARTER.md` and `STATE.md`: the operating frame and the live ledger.
4. `docs/DECISIONS/`: why things are the way they are. ADR-016 is the current frame.
5. `docs/PRODUCT.md` and `docs/ARCHITECTURE.md`: per-tab spec and the engineering contract.
6. `docs/ICP.md` and `docs/icp.json`: who the OS sources, scores and drafts for. `api/_icpScore.ts` is the executable copy.
7. `docs/CONTENT-ENGINE-BUILD-SIGNALS.md`: how this repo's commits become content.
8. `AGENTS.md`: rules for coding agents working in this repo (house systems, tests, CI).

## Do not trust

- `docs/ICP.md`, section "The six v2 lanes" and below: the v2 lane detail from 2026-06-20. Lane order and which lanes are live or parked are the table at the top of that file and `api/_icpScore.ts` (ADR-016, 2026-09-06).
- `n8n/workflows/README.md`: its 2026-05-25 claim that the n8n cloud editor is canonical was superseded by `scripts/n8n/README.md` (git wins). Banner added 2026-09-07; the snapshots stay as history.
- `docs/CONTENT_TAB_SPEC.md`: the June 2026 build spec, superseded taxonomy, banner in the file.
- `docs/pr-2` to `docs/pr-8` and `docs/visibility-followups-2026-05.md`: change notes from the May rebuild, a changelog supplement, not architecture.
- Any doc that names a VPS or Drive copy of the architecture doc: gone since 2026-09-07.
