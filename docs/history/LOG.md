# History log

Newest first. Entries are written by the docs steward (`docs/steward/RUNBOOK.md`)
and by people doing the same job by hand. Nothing in this file describes
current behaviour; `NOW.md` and `docs/plans/one-swing/STATE.md` do. Files
moved here keep their body verbatim under a Historical banner.

## 2026-09-21

- reconciled at `27d6aa67`: fifteen non-steward commits since `c3257cd0`. Krish moved COMPOUND to its own repository (`krishanraja/compound`), taking `compound/`, its pipeline workflows, schema migrations and edge functions with it; the repo's own docs (`AGENTS.md`, `README.md`, `docs/README.md`, `docs/DECISIONS/README.md`, `docs/TESTING.md`, `docs/PROMPT-QUALITY-AUDIT.md`, `docs/MINDMAKE_OS_ARCHITECTURE.md`) were already reconciled in that same commit, so no drift was found there. The fleet's own Anthropic spend (46 n8n nodes across 33 workflows) joined `meter_daily` for the first time. Two silent-failure bugs of the same shape were fixed: the usage meter discarding its own write errors, and the learning bank having a writer for machine suggestions with none for Krish's verdict on them. A new Claude artifact, `artifacts/triage-desk.html`, runs the content triage/dry-run/read-back loop on the viewer's own subscription rather than the metered API key, and surfaced four more silent-failure bugs while it was built. `NOW.md`'s stale guard count (seventeen, actually nineteen per `.github/workflows/ci.yml`) is corrected; re-headed with four bullets.

## 2026-09-20

- reconciled at `8b30ae99`: five non-steward commits since `404a805b`, all closing out follow-ons from the coordinated format rename. `FACTORY_FANOUT`, `DEFAULT_CHANNELS` and `FORMAT_ADAPTS` in `src/lib/contentEngine.ts` were the last format-vocabulary constants still carrying a retired name or a made-up state string; the composer's research picker, the deep research chips, the mobile Queue fallback and `ShiftsRoom`'s caption were the last surfaces printing one. `docs/CONTENT-ENGINE-OPERATING-GUIDE.md` is new; the black plate placeholder for an undrawn wordmark is gone from the degraded branch. Outside the taxonomy: all 23 Gemini fallback nodes were quietly calling models Google had retired months ago, fixed to `gemini-3.6-flash`, and two n8n snapshots were flagged stale before a sync could overwrite the live workflows. `NOW.md`'s "Where it is right now" and "Do not trust" sections still said the format vocabulary was unaligned, two reconciliations after `037261f4` had already fixed it; both corrected, and `PUBLIC_SERIES`'s reason for still carrying `built` / `paid` (it is the wordmark asset registry, not the taxonomy) is now stated on its own rather than read as the same bug. `NOW.md` re-headed with five bullets.
- reconciled at `c9b295a1`: the RE Dossier Engine stopped writing error objects into dossiers, and the reason the content pipeline had gone quiet was traced to the same dead Anthropic key rather than to the sources themselves. The waiting list was also corrected: it still carried the finished coordinated rename and the withdrawn Vercel action as live asks.
- reconciled at `0dd4ab3f`: the three Marcus workflows moved off raw httpRequest calls onto n8n's native Anthropic node after a live run proved every Anthropic credential in the instance was rejected, not just the stale one. Verified one workflow end to end before touching the other two, because the risk was the downstream parse rather than the call. An audit of all 26 Anthropic nodes corrected my own claim that 26 needed fixing: 22 were correct by design and two were genuinely writing error objects into dossiers.
- reconciled at `037261f4`: the coordinated format rename landed and `check-content-taxonomy` passes for the first time since 2026-09-17. Four hardcoded lists became one reader over a snapshot of `venture_formats`; two consequences that could not be renamed away (no corpus playbook for mind.the.gap, no wordmark for any subchannel) are declared in code with guards that fail if the declaration goes stale or outlives the gap. Migration `20260920100000` gave every intake source a declared cadence and turned an overdue one into a named hand-off row, after four live sources were found silently quiet, one of them for 31 days. `openclaw-vps` became a retiring clock rather than an expected one. `NOW.md` re-headed with three bullets.
- note: `PublicSeriesKey` stays two-valued. Widening it means a third Content room and three new wordmarks, which is a product change rather than a rename, so it is named in `src/lib/publicSeries.ts` and left for Krish. The two binary ternaries in `contentRouting.ts` and `editorialOpportunities.ts` that make a third format unrepresentable are named in the same place.
- reconciled at `66881a39`: three non-steward commits since `69376a2f`, all closing gaps the previous session's own migrations opened. `20260919140000` bridges the three read ledgers that had been backfilled into `intake_items` and then left without triggers, which had already drifted by two rows in two hours; every ledger reads whole afterwards (625 rows, four sources, zero unmirrored). A `revoke`/`grant` pair closed a `SECURITY DEFINER` function that PostgREST had published to anon, found by a check agent after the migration was applied rather than before. The architecture doc's section 0b gained the one recorded pull-only exception, the weekly slate link, with the bounds that keep it one exception rather than a precedent: one recipient, one payload, one trigger. `NOW.md` re-headed with three bullets.
- note: the first slate was proposed the same day and its 39 rows are in `public.suggestions` under `run_id = 'first-slate:2026-W38'`. The rows are in this database; the machinery that produced them is in `makeyourmindup`, `engine/runs/2026-W38` and `apps/machine/slate-page`. Nothing has been published, sent or scheduled from them.

## 2026-09-19

- reconciled at `69376a2f`: nine non-steward commits since the last reconciliation (`954f21b4`), all from one session on the publication's format vocabulary and the machinery around it. Two migrations landed and were applied: `20260919100000` (the learning bank: `suggestions`, `suggestion_verdicts`, `autonomy_ladder`, `handoff_reasons`, with the anti-echo rule held by a CHECK) and `20260919120000` (one intake: `intake_items` and its vocabularies, `content_ideas.source_type` moved from a seventeen-literal CHECK to a foreign key, 623 rows backfilled and asserted). The n8n factory mirror was synced after the live switch gained the three subchannel slugs. `docs/MINDMAKE_OS_ARCHITECTURE.md` section 3.4.1 was corrected on the execution governor (warn-only since 2026-09-09, no automated cap), and `scripts/cron/crontab.txt` was regenerated from a read-only audit run on the host (33 entries where the snapshot held 12). `NOW.md` re-headed with three new "Where it is right now" lines, five "What changed recently" bullets, one "waiting on Krish" line naming seven credentials, and two "Do not trust" lines.
- note: `scripts/check-content-taxonomy.mts` fails on main by design (eight retired labels in four lists) until the coordinated rename lands; the guard now reads `label:` and would have caught the 2026-08-26 batch it missed. `check-content-taxonomy` is deliberately not in CI for that reason (AGENTS.md).

## 2026-09-16

- reconciled at `954f21b4`: eight non-steward commits since the last reconciliation (`6a7f9ffd`). The largest was the Room-to-Pilots rename (ADR-023, migration `20260916100000`): the classifier prompt that had been writing "room" into every generated ask line, the table, its two renamed ladder states, the scorecard column, the job ids, the ships dedup key and the routes all moved to `pilot_deals` / Pilots, with `?lane=room` kept resolving. Most of the prose (`NOW.md`, `docs/PRODUCT.md`, `docs/DATABASE.md`, `docs/ICP.md`, `docs/icp.json`, `docs/plans/one-swing/CHARTER.md`) was already corrected by the commit that made the change; this run closed what it missed: the "Lane: People → Room" header and its `RoomBody`/"Room booked"/`room:<id>` internals in `docs/PRODUCT.md`, the same column names in `docs/DATABASE.md`, and `docs/plans/one-swing/STATE.md`'s ledger row and stamp (last checked 2026-09-11). Two same-day follow-on commits (`77ded494`, `c444337e`) then replaced the phone pager with the shared `TriageDeck` swipe across the listed and drafted lanes and fixed a drafted approach with no email rendering no contact action at all; `docs/COMPONENTS.md`'s `MobilePilots` section and `docs/TESTING.md`'s `pilots-noscroll.spec.ts` row still described the pager and are corrected. A final commit (`954f21b4`) taught `network_search` to demote a flagged competitor instead of only ranking them, and fixed a leaked vendor HTTP error string on the Network tab; `docs/DATABASE.md` already carried the new `contact_intelligence` columns from the same commit. `NOW.md` re-headed to `954f21b4` with two new "What changed recently" bullets.
- note: the rename migration did not touch `growth_aeo_subjects.room_target_id` (still literally named `room_target_id`, FK now pointing at `pilot_deals` by OID). Left as-is in `docs/MINDMAKE_OS_ARCHITECTURE.md`, which still says "Room target" there - that prose matches the actual column name, so changing it would be the drift, not the fix. Flagged here for whoever next touches that migration chain.

## 2026-09-15

- reconciled at `6a7f9ffd`: 29 non-steward commits since the last reconciliation (`d015491b`), nearly all of them the Network lane rebuild (ADR-021, ADR-022: unconditional LinkedIn reach, typed `contact_intelligence` enrichment columns, an eight-tier intent stance ladder, a mandatory contact-import gate, one-click channel-aware contact, the `mindmake_wedge` tier, the hunter browser-extension payload route) plus search-stability and honesty fixes found along the way. `docs/PRODUCT.md`, `docs/AGENTS.md`, `docs/ORPHANED-COMPONENTS.md` and `docs/plans/one-swing/STATE.md` were each touched by the PR authors themselves for the one line each needed (the Hunt lane's `VITE_BRIDGES_LANE_ENABLED` retirement), but `docs/PRODUCT.md` had no "Lane: People → Network" section at all despite Network being the tab's default lane since 2026-08-22 - a gap that predates this range and is now closed with a Purpose/Layout/Inputs/Writes/Behaviour rules/States section, sourced from ADR-021, ADR-022 and the 29 commit bodies. `NOW.md` re-headed to `6a7f9ffd` with seven new "What changed recently" bullets and two rewritten "Where it is right now" lines (the Network lane's new guarantees, and the Hunt lane's fuller PR #323 detail: the eight-day invisible gap, applied-role status, and the payload/extension chain). No document moved; no contradiction found that the code could not settle.
- note: two migrations landed with an identical timestamp prefix, `20260915250000_cap_retrieval_text_length.sql` and `20260915250000_enriched_degraded_is_a_status.sql`, from two unrelated PRs (#344, #343). Both applied cleanly; flagged here as a naming collision worth avoiding, not a defect to fix by this run.

## 2026-09-14

- reconciled at `d015491b`: eight non-steward commits since the last reconciliation (`9df9bef9`) had documentation consequences, and every one of them was already fixed in the same commit by the PR that made the change (`docs/AGENTS.md`, `docs/GLOSSARY.md` and `docs/MINDMAKE_OS_ARCHITECTURE.md` all correct against the roster and fallback work). `NOW.md` was the one file left stale, re-headed to `d015491b` with three new bullets: the Anthropic fallback repair and Priya's retirement (PR #316), the n8n Cloud reconciliation that followed it (PRs #317, #318), and the Kai row fix, the Hunter correction and the first runtime secrets scan (PR #320: 217 secret literals across 66 workflows, 16 distinct live credentials, rotation now recorded under "waiting on Krish"). `docs/steward/fleet.json`'s `content-engine` rename (from `mindmake-video-studio`) was made by the repo's own maintainers (PR #319), not this run.

## 2026-09-09

- reconciled at `74a9c76e`: six commits since the last reconciliation (`caa34027`) had documentation consequences. `docs/SECURITY.md`, `docs/DB_HEALTH.md` and `docs/DATABASE.md` were stale on two points: the "Today" auth table said `/api/*` write routes carried no auth (75 of 141 now call `guard()`, PR #308), and the `USING(true)` write-policy row said tightening was blocked on the ADR-008 auth cutover (24 tables were narrowed directly on 2026-09-09 without it, PR #306, because they had no anon caller in the codebase at all). `docs/OBSERVABILITY.md`'s tier-4 row said a failure cluster still wrote a `corrections` row; it is report only since Krish's ruling the same day. `docs/CONTENT-ENGINE-V2-SPEC.md` gained a dated addendum for the Content tab layout and routing fix (PR #307) and the obligation strip's Run again button (PR #305). `NOW.md` re-headed to `74a9c76e` with six new bullets.
- note: `docs/steward/RUNBOOK.md` and `docs/steward/SCHEMA.md` stamps bumped to 2026-09-09 after checking their bodies against the current `.github/workflows/docs-steward.yml` and `scripts/steward/validate.mjs`; no content drift found in either.

## 2026-09-07

- decision (Krish, 2026-09-07): the docs steward is adopted across eight repos. `NOW.md` at the root is the one file agents are promised is current; this log is where superseded material goes instead of being deleted. Procedure: `docs/steward/RUNBOOK.md`. The three weekly "Documentation Refresh" Routines that wrote to unmerged `claude/*` branches were deleted by Krish the same day (recorded in `docs/MINDMAKE_OS_ARCHITECTURE.md` section 0c, #281).
- reconciled at `3554502d`: main moved by five commits while the seven other fleet repos were being bootstrapped (#280, #281, the scorecard author-filter change, its guard, and a merge). NOW.md re-headed and given the two bullets; `scripts/steward/validate.mjs` and `digest.mjs` now pass `core.quotePath=false` to git after an en-dash filename in mm-ctrl showed quoted paths fail the allowlist glob; `fleet.json` corrected to `public/.well-known/ai-plugin.json` for fractionl-pulse.
- reconciled at `600ba77c`: `docs/icp.json` moved from v2 to v3. It still carried the six-lane model of 2026-06-20 while `api/_icpScore.ts` had led with the `room_face` lane and parked three lanes since 2026-09-06 (ADR-016). Added the `room_face` lane with the scorer's weights and `parked: true` on `fractional_network`, `mm_ctrl_buyer` and `ecosystem_partner`. `docs/ICP.md` gained a lane-status table and a v3 status line; its v2 body is kept as the reference for the six lanes.
- reconciled at `600ba77c`: `n8n/workflows/README.md` claimed the n8n cloud editor was canonical (snapshot of 2026-05-25) while `scripts/n8n/README.md` and the live sync tooling say git wins. A banner at the top of the snapshot README now says so; the snapshots stay as the historical record they are.
- reconciled at `600ba77c`: `docs/GLOSSARY.md` scope now points at `docs/MINDMAKE_OS_ARCHITECTURE.md` section 18 for OS-wide terms and says which wins.
- note: steward and bootstrap commits are signed by the cloud session (`Claude <noreply@anthropic.com>`) rather than the `Krish Raja <hello@krishraja.com>` author standard in `docs/CONTRIBUTING.md`. Recorded here rather than hidden; the Actions steward pushes with the default token.
