# Optional server-side override. Production normally receives a short-lived
# Vercel OIDC token from the COMPOUND proxy instead of storing an LLM key.
COMPOUND_LLM_BASE_URL=
COMPOUND_LLM_API_KEY=
COMPOUND_LLM_MODEL=
